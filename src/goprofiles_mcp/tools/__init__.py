# GoProfiles MCP tools live in this package.
# Add a module per tool (or tool group), then register it in goprofiles_mcp.server.
# Start from _template.py — it documents the conventions and has a working
# read-only search tool to copy.
